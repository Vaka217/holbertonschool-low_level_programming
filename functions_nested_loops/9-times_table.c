#include "main.h"
#include "_times_table.c"
/**
 * timetable - prints the 9 times table, starting with 0.
 *
 * Description: iterates with two for statemantes through rows and columns,
 * printing the numbers and verifying with if statements how many spaces after
 * the comma.
 * Return: Always 0.
 */
int timetable(void)
{
	    times_table();
	        return (0);
}

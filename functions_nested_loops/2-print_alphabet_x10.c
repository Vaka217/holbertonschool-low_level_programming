#include "main.h"
#include "_print_alphabet_x10.c"
/**
 * abcx10 - Prints the alphabet in lowercase
 *
 * Description: putchar prints the value of the char variable c, c initializes
 * with 'a' value and the for statement iterates until c ends with 'z' value
 * and uses putchar with each value of c. Then a while iterates over
 * to print the alphabet 10 times.
 * Return: Always 0.
 */
int abcx10(void)
{
	print_alphabet_x10();
	return (0);
}


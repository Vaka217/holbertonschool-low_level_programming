#include "main.h"
#include <stdio.h>
#include "_add.c"

/**
   * adds - check the code
    *
     * Return: Always 0.
      */
int adds(void)
{
	    int n;

	        n = add(89, 9);
		    printf("%d\n", n);
		        return (0);
}
